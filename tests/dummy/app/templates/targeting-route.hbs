This is a route for acceptance tests that has route-based targeting set.
<br>
{{#link-to 'non-targeting-route'}}Route with No Targeting{{/link-to}}
<br>
{{#link-to 'component-targeting-route'}}Route with Component-based Targeting{{/link-to}}
<br>
If you're looking at this in a browser you can try the following console commands:
<pre>
  googletag.showConsole()
  googletag.pubads().getTargetingKeys()
  googletag.pubads().getTargeting(&lt;key&gt;)
</pre>